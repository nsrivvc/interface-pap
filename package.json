{
  "name": "interface-pap",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Value Creed styled data pipeline interface (React + Express + Neon)",
  "dependencies": {
    "jsonwebtoken": "^9.0.2"
  },
  "scripts": {
    "dev": "concurrently -n server,client -c blue,yellow \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "db:init": "npm run db:init --prefix server",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
